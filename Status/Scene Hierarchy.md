# Scene Hierarchy

Current scene:

```txt
Assets/Scenes/SampleScene.unity
```

This document reflects the current implemented MVP state after Prompt06 completion.

## SampleScene Root Hierarchy

```txt
SampleScene
├─ Main Camera
├─ Directional Light
├─ Global Volume
├─ GameManager
├─ BoardRoot
├─ PiecesRoot
├─ PreparationRoot
├─ MovementRoot
├─ InteractionRoot
├─ CooldownRoot
├─ EnemySetupRoot
├─ PrototypeUIRoot
└─ EnemyAIRoot
```

## Root Objects and Components

### Main Camera

Components:

```txt
Transform
Camera
AudioListener
UniversalAdditionalCameraData
```

Purpose:

```txt
Fixed camera for 3D board and 2D sprite pieces.
```

### Directional Light

Components:

```txt
Transform
Light
UniversalAdditionalLightData
```

Purpose:

```txt
Basic scene lighting.
```

### Global Volume

Components:

```txt
Transform
Volume
```

Purpose:

```txt
URP scene volume.
```

### GameManager

Components:

```txt
Transform
GameManager
PlayerHealthService
```

Purpose:

```txt
Owns core game state flow.
Owns player health service.
Transitions through Boot, StageStart, Preparation, Playing, StageClear, and GameOver.
```

Implemented flow:

```txt
Boot / Title
→ StageStart
→ Preparation
→ Playing / First Move Phase
→ Playing / Enemy AI Active
→ StageClear / GameOver
```

Key calls:

```txt
StartStage()
StartPreparation()
StartBattle()
NotifyEnemyKingCaptured()
NotifyPlayerKingCaptured()
```

Player King capture retry:

```txt
Playing + Player King Captured
→ HP -1
→ HP > 0
→ Preparation
→ Preserve selected loadout
→ Preserve active enemy setup
```

### BoardRoot

Components:

```txt
Transform
BoardManager
BoardView
```

Runtime children:

```txt
Board Tiles
└─ Tile_x_y objects generated by BoardView
```

Purpose:

```txt
Stores board size and occupancy.
Converts between grid and world coordinates.
Generates simple 3D tile visuals.
Applies placement, selection, movement, and capture highlights.
```

Current board default:

```txt
Width = 6
Height = 6
```

Placement rows:

```txt
Player: y = 0, y = 1
Enemy: y = boardHeight - 2, y = boardHeight - 1
```

### PiecesRoot

Components:

```txt
Transform
```

Runtime children:

```txt
Player_* pieces created by PreparationManager
Enemy_* pieces created by EnemySetupManager
```

Purpose:

```txt
Parent object for runtime-created piece GameObjects.
```

Runtime piece components:

```txt
Transform
PieceController
PieceView
PieceCooldown
SpriteRenderer child created by PieceView when needed
```

### PreparationRoot

Components:

```txt
Transform
PreparationManager
PlacementValidator
ManualPlacementController
```

Purpose:

```txt
Handles pre-battle loadout and placement.
Validates loadout cost.
Requires King.
Prevents placement overlap.
Restricts player placement to bottom two rows.
Calls GameManager.StartBattle() only when preparation is valid.
Requires exactly one selected and placed Player King.
Allows placed player pieces to be repositioned during Preparation.
Preserves the selected loadout after a non-terminal Player King capture.
```

Current MVP loadout example:

```txt
King + Rook + Knight + Pawn + Pawn = 7
```

### MovementRoot

Components:

```txt
Transform
PieceMovementService
```

Purpose:

```txt
Calculates valid movement positions for selected pieces.
Delegates movement rules to the current piece movement behavior.
```

Implemented movement behaviors:

```txt
KingMovement
RookMovement
KnightMovement
PawnMovement
```

### InteractionRoot

Components:

```txt
Transform
PieceActionController
BoardInputController
```

Purpose:

```txt
Executes selected piece movement.
Handles capture.
Starts cooldowns after successful movement.
Notifies GameManager when a King is captured.
Reads mouse clicks through the New Input System.
Raycasts from the camera to BoardTileView colliders.
Routes Preparation clicks to ManualPlacementController.
Selects player pieces and displays valid movement or capture highlights during Playing.
Executes valid destinations through PieceActionController.
```

Important current note:

```txt
The project uses Input System-only settings.
BoardInputController uses Mouse.current for primary click and pointer position input.
Gameplay selection and movement input is accepted only during Playing.
Enemy pieces cannot be selected.
Invalid destinations keep the current selection and do not start cooldowns.
Successful moves clear selection and use existing cooldown behavior.
Legacy Input Manager support remains conditionally available.
```

### CooldownRoot

Components:

```txt
Transform
PlayerGlobalCooldown
```

Purpose:

```txt
Tracks player global cooldown after a player piece moves.
```

Current global cooldown behavior:

```txt
Duration is clamped to 1-3 seconds.
Player pieces cannot move while global cooldown is active.
```

### EnemySetupRoot

Components:

```txt
Transform
EnemySetupManager
```

Purpose:

```txt
Chooses and spawns lightweight enemy setup patterns.
Preserves ActiveSetup during a Player King capture retry.
Respawns the same encounter when the next retry battle begins.
```

Current enemy patterns:

```txt
PatternA_KingRookPawn
PatternB_KingKnightPawnPawn
PatternC_KingRookKnight
```

Spawn validation:

```txt
Enemy pieces must be placed in the top two board rows.
Overlapping enemy placement is rejected.
```

### PrototypeUIRoot

Components:

```txt
Transform
PrototypeHud
```

Purpose:

```txt
Provides editor-testable prototype controls and status display.
Provides Title, Preparation, Playing, and Result state presentation.
```

HUD displays:

```txt
Current state
Player HP
Manual placement selection
Loadout cost
Placed count
Enemy setup name
Enemy count
Global cooldown remaining time
StageClear / GameOver result banner
First move battle notice
Global cooldown progress bar
Selected piece name, type, and cooldown
Enemy team composition
Enemy AI waiting / active state
```

HUD buttons:

```txt
Start Game
King
Rook
Knight
Pawn
Setup Player MVP
Spawn Random Enemies
Start Battle
Reset Preparation
Restart
Return to Title
```

### EnemyAIRoot

Components:

```txt
Transform
EnemyAIController
```

Purpose:

```txt
Runs lightweight reactive enemy AI during Playing state.
Uses existing movement, board, capture, cooldown, and game state systems.
```

Current AI behavior:

```txt
Capture Player King immediately.
Move each Enemy Pawn once during opening if possible.
Prefer captures by non-King enemy pieces.
Prefer random movement by non-King enemy pieces.
Use Enemy King movement only as fallback unless it can capture Player King.
Wait if no valid movement exists.
```

First move phase:

```txt
Start Battle
→ Enemy AI remains inactive
→ Player completes first successful move
→ First move notice is removed
→ Enemy AI becomes active
```

The first move phase is reset after a Player King capture retry.

First move activation source:

```txt
PieceActionController.OnPieceMoved
→ Successful Player move
→ EnemyAIController releases first-move gate
```

Invalid movement attempts and elapsed time do not release the gate.

## Runtime Object Creation

### Board Tiles

Created by:

```txt
BoardView.GenerateBoard()
```

Each generated tile has:

```txt
Transform
MeshRenderer
BoxCollider
BoardTileView
```

Tile names:

```txt
Tile_0_0
Tile_1_0
...
Tile_5_5
```

### Player Pieces

Created by:

```txt
PreparationManager.TryPlaceSelectedPiece(...)
PrototypeHud.SetupPlayerMvpLoadoutAndPlacement()
ManualPlacementController.HandleTileClicked(...)
```

Name pattern:

```txt
Player_{DisplayName}_{LoadoutIndex}
```

### Enemy Pieces

Created by:

```txt
EnemySetupManager.SpawnRandomSetup()
EnemySetupManager.SpawnSetup(...)
PrototypeHud.SpawnRandomEnemies()
```

Name pattern:

```txt
Enemy_{DisplayName}
```

### Enemy AI Actions

Executed by:

```txt
EnemyAIController
EnemyMoveSelector
PieceActionController.TryMoveSelectedPiece(...)
```

Enemy AI action logs:

```txt
Enemy AI opening pawn move
Enemy AI capture
Enemy AI random move
Enemy AI waits
```

## Current Scene Test Flow

Recommended editor flow:

```txt
1. Enter Play Mode
2. Confirm the Title screen is visible
3. Click Start Game and confirm Preparation
4. Click Setup Player MVP
5. Click Spawn Random Enemies
6. Click Start Battle
7. Confirm the First Move Notice and Enemy AI waiting state
8. Confirm Enemy AI does not move before a successful player move
9. Click a player piece and confirm selected piece information
10. Click a valid destination to complete the first move
11. Confirm the notice disappears and Enemy AI becomes active
12. Confirm the global cooldown bar updates
13. Capture Enemy King to reach StageClear
14. Allow Player King capture to verify retry and GameOver flows
15. Use Restart or Return to Title to replay
```

Current verified results:

```txt
Board appears in Play Mode
Prototype HUD appears in Play Mode
Player MVP setup places pieces on valid rows
Random enemy setup places enemies on top rows
2D sprite pieces appear above the 3D board
Enemy AI moves automatically during Playing
Enemy AI uses existing movement and capture rules
Enemy AI respects individual piece cooldowns
Enemy AI can capture player pieces
Enemy AI prioritizes Player King capture
Enemy AI moves Enemy Pawns once during opening when possible
Enemy AI reduces early Enemy King aggression
Player pieces can be selected with the mouse
Enemy pieces cannot be selected
Selected tiles display selection feedback
Valid movement and capture destinations display highlights
Invalid destinations do not move the selected piece
Valid destinations execute movement
Player movement starts individual and global cooldowns
StageClear blocks further player interaction
Enemy King capture reaches StageClear
Player King capture damages player HP
Cooldowns start after successful movement
Enemy AI does not move before the first successful player move
Invalid player movement does not end the first move phase
First successful player movement enables Enemy AI
First move notice is visible only while Enemy AI is gated
Global cooldown progress is visible during Playing
Selected player piece information updates during Playing
Enemy composition matches the active enemy setup
Player King retry preserves loadout and enemy setup
Player King retry returns to the first move phase
```

## Known Scene Limitations

Not implemented in scene:

```txt
Drag-and-drop placement
Touch controls
Advanced input architecture
Keyboard navigation
Advanced UI screens
Per-piece cooldown bars
Sound
Animation
VFX
Shop UI
Reward UI
Stage transition UI
Save / Load UI
Check
Checkmate
Threat maps
Future turn prediction
Advanced tactical analysis
```

Current scene is intended for MVP editor testing, not final game presentation polish.
